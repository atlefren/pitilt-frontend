import React from 'react';

export default function CreatePlot(props) {
    return (
        <form>
         <fieldset>
            <legend>Create New Plot</legend>
        </fieldset>
        </form>
    );
}